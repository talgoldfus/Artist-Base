<h1><%= "#{@artist.name}'s Profile" %></h1>
<h3>In a few words...</h3>
<p><%= @artist.abstract %></p><br>

<h3>Bio:</h3>
<p><%= @artist.bio %></p><br>

<% @artist.image_collections.each do |collection| %>
  <p><%= link_to "#{collection.name}", collection %></p>
<% end %>

<% if session[:artist_id] %>
<%= link_to "edit artist", edit_artist_path(@artist) %><br>
<%= link_to "Add New Collection", new_image_collection_path %><br>
<% end%>
