<br>
<br>

  <div class="jumbotron">
    <div class=" container-fluid col-xs-4">
      <div class="panel-body">
        <%= image_tag "#{@fan.img_link}" %>
      </div>
    </div>
    <div class=" container-fluid col-xs-8">
        <h2> Name: <%= @fan.name %></h2>
        <br>
        <h3>From:<%="#{@fan.city}, #{@fan.state}" %></h3>
        <br>
        <h3>Bio: <%= @fan.bio %></h3>
        <br>
    </div>
    <div class="text-right">
    <%= link_to "Edit Profile", edit_fan_path(@fan), class: "btn btn-primary" %>
    </div>
  </div>